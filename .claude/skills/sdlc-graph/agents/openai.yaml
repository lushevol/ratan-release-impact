interface:
  display_name: "SDLC Graph"
  short_description: "Build, query, and expose cross-repo impact graphs"
  default_prompt: "Use $sdlc-graph to generate the evidence-backed architecture bundle and analyze requirement or code impact through its MCP tools."
policy:
  allow_implicit_invocation: true
