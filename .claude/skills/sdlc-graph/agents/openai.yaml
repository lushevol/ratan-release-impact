interface:
  display_name: "SDLC Graph"
  short_description: "Build, trace, and visualize cross-repo graphs"
  default_prompt: "Use $sdlc-graph to generate and visualize the complete evidence-backed architecture graph for these repositories."
policy:
  allow_implicit_invocation: true
