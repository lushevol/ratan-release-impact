[project]
name = "ratan-test-engine"
version = "0.1.0"
description = "Local CCIL and ReBook behavior simulation for the SDLC POC"
requires-python = ">=3.9"
dependencies = [
    "robotframework==7.4.2",
]

[tool.uv]
package = false
