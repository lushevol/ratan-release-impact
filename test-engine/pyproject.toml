[project]
name = "standalone-ratan-subsets"
version = "0.1.0"
description = "Self-contained CCIL and ReBook Robot Framework extracts"
requires-python = ">=3.9"
dependencies = [
    "robotframework>=7.2.2",
]